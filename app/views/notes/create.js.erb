<% if @group_notes.present? %>

$("#chat_area").html("<%= j(render 'shared/chatroom_content', { note: @group_notes }) %>")

<% else %>

$("#my_notes_area").html("<%= j(render 'shared/my_content', { note: @own_notes }) %>")

<% end %>

$("input.note_form").val('');
$('.js-upload-file').val('');
$('.js-upload-filename').text('');
$(this).hide();
$('.fa-times-circle').hide();
$('.fa-image').removeAttr('id');
